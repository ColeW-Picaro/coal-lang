let a : int = 2;
let b : int = 3;
let name : string = "cole";
let nameab : string = name;

let max (a : int, b : int) : int do
  if (a > b)
    return a;
  else
    return b;
end
let m : int = max(a, b);

for (let i : int = 0; i < 10; ++i) m = m + i;

for (let i : int = 0; i < 10; ++i) m = m + i;

return 0;
