let a : int = 2;
let b : int = 3;
let add (a : int, b : int) : int do
  return a + b;
end

let max (a : int, b : int) : int do
  if (a > b)
    return a;
  else
    return b;
end

max(a, b);
return 0;
