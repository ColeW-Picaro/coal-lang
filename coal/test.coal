  let min (a:int, b:int) : int do
    if (a < b)
      return a;
    else
      return b;
  end

  let foo (a:int) : string do
    let sum : int = a + a;
    let prod : int = a * a;
    let b : int = b / 1;
    let bar (b:int) : int do
      return a + b;
    end
    return bar (a);
  end

  let c : string;
  let name : string = "Hello, world!";
  name = 4;
  return 0;
